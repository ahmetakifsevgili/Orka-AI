# ── .NET ──────────────────────────────────────────────────────────────────────
bin/
obj/
publish/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
*.DotSettings.user

# User Secrets — asla commit'lenmez
# Windows: %APPDATA%\Microsoft\UserSecrets\
# Bu satır sembolik; user-secrets zaten repo dışında saklanır.

# Local appsettings overrides — production anahtarı içerebilir
appsettings.*.json
!appsettings.json
!appsettings.*.template.json

# EF Core migrations snapshot geçici dosyalar
*.migration.cs.bak

# ── Node / Frontend ────────────────────────────────────────────────────────────
node_modules/
dist/
.vite/
Orka-Front/dist/

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
*.swp
*.DS_Store
Thumbs.db

# ── Geliştirici scratch / geçici dosyalar ────────────────────────────────────
scratch/
.tmp/
.dotnet-home/
Orka-Front/refactor.py
*.mdf
*.ldf

# ── Ortam değişkenleri ────────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── Claude Code ───────────────────────────────────────────────────────────────
# Kişisel tercihler & geçici notlar (asla commit'lenmez)
CLAUDE.local.md
.claude/settings.local.json
.claude/local/

# ── Test & denetim çıktıları ──────────────────────────────────────────────────
scripts/reports/
scripts/llm-eval/output/
.test-results/
Orka-Front/test-results/
build_out.txt
build_output*.txt
clean_build*.txt
infra_build*.txt
solution_build*.txt
tests_infrastructure*.txt
*.log
!.claude/*.log
Orka.API/build.txt
Orka.API/dotnet_log.txt

# Python test cache
__pycache__/
*.pyc
.pytest_cache/

# V1 closure local artifacts
.tmp-artifacts/
temp/
brain/
testsprite_tests/tmp/
TestResults/
coverage/
coverage.json
*.trx
*.binlog
*.coverage

# Local generated/reference workspace noise
artifacts/
repo/
life_tests/reports/
